<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<div id="home-list">
  <ul id="pedal-home-list">
    <% @pedals.each do |pedal| %>
      <li>
        <%= pedal.cached_votes_score %>
        <%= link_to pedal.title, pedal %>
  
      </li>
    <% end %>
  </ul>
  
  <ul id="rig-home-list">
    <% @rigs.each do |rig| %>
      <li>
        <%= rig.cached_votes_score %>
        <%= link_to rig.guitarist, rig %>
  
      </li>
    <% end %>
  </ul>
</div>